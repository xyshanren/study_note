---
url: https://www.delftstack.com/zh/howto/python/anaconda-check-python-version/#google_vignette
source: www.delftstack.com
date_saved: 2026-04-23
tags: Python, 学习-教程
content_type: article
word_count: 2095
status: 已处理
---

# 检查 Python 和 Anaconda 版本 | D栈 - Delft Stack

> source: [www.delftstack.com](https://www.delftstack.com/zh/howto/python/anaconda-check-python-version/#google_vignette)

## 核心要点

1. conda --version

conda -V

同样，我们使用以下命令在 Anaconda 提示符下检查 Python 版本
2. python --version

python -V

我们还可以使用以下命令在 Anaconda 提示符下确定 Anaconda 和 Python 版本
3. 使用 conda -V 命令检查 Anaconda 版本

在 Anaconda 提示符下，发出 conda --V 命令以检查 Anaconda 版本

## 内容预览

使用 conda -V 命令检查 Anaconda 版本

 使用 conda --version 命令检查 Anaconda 版本

 使用 python -V 命令检查 Python 版本

 使用 python --version 命令检查 Python 版本

 使用 conda list anaconda$ 命令检查 Anaconda 版本

 使用 conda list 命令检查 Anaconda 和 Python 版本

 使用 conda info 命令检查 Anaconda 和 Python 版本

我们可以在 Anaconda 提示符下使用以下命令来确定 Anaconda 版本。

conda --version

conda -V

同样，我们使用以下命令在 Anaconda 提示符下检查 Python 版本。

python --version

python -V

我们还可以使用以下命令在 Anaconda 提示符下确定 Anaconda 和 Python 版本。

使用 conda list anaconda$ 命令。

使用 conda list 命令。

使用 conda info 命令。

使用 conda -V 命令检查 Anaconda 版本

在 Anaconda 提示符下，发出 conda --V 命令以检查 Anaconda 版本。

...

## 我的判断

- 价值：待评估
- 适用场景：
- 可信度：
